# SmartScholar Frontend

## Run with real backend

1. Create env file:

```bash
cp .env.example .env
```

2. Install and run:

```bash
npm install
npm run dev
```

3. Ensure `VITE_API_BASE_URL` points to backend API.

Default:

```env
VITE_API_BASE_URL=http://localhost:3001/api
```
